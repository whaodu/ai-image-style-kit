---
name: doubao-image-ops
description: |
  火山引擎豆包图像工具。触发条件：①提取风格 - 用户发来图片时，提取并保存风格；②生成图片 - 用户说"用xxx风格生成图片"时，先确认风格并引导用户输入主题描述，再融合生成。
  触发词：提取风格、分析风格、分析图片风格、图片生图、生图、生成图片
---

# 火山引擎图像工具箱

## 功能概述

| 功能 | 模型 | 说明 |
|------|------|------|
| 风格分析 | `doubao-seed-2-0-pro-260215` | 上传图片，提取风格特征并自动保存 |
| 图像生成 | `doubao-seedream-5-0-260128` | 文本描述生成图片 |

## 存储机制

风格文件保存于 `scripts/styles/style_XXX.json`，每个文件包含：
- `id`：风格编号（自动递增）
- `style_name`：风格名称（用户自定义，可为空）
- `style_description`：风格描述
- `source_image`：图片来源
- `saved_at`：保存时间

## 环境配置

**API Key 存储位置：** 参见 `TOOLS.md`（不写在代码里）
```bash
export ARK_API_KEY="你的火山引擎 ARK API Key"
```

获取方式：火山引擎控制台 → API Key 管理

## 完整工作流程

```
第1步：发图片 → 提取风格（可命名）
第2步：用户说"用xxx风格生成图片" → AI确认风格 → 引导用户输入主题 → 融合生成图片
```

---

## 对话式交互（推荐方式）

用户可以用自然语言描述需求，**不需要记住风格编号**，AI 自动解析。

### 提取风格
```
用户发来图片 + 可选说"提取风格"/"分析风格"/"保存为xxx风格"
```
AI 自动调用 `analyze`，返回风格编号和名称。

### 用风格生成图片（三步流程）
```
用户："用【风格名】风格生成图片"
用户："用科技感风格生成图片"
```
**第一步**：AI 识别风格，确认风格，提示用户输入主题描述
**第二步**：用户输入描述 → AI 分析提炼提示词
**第三步**：融合风格 → 生成图片

```
用户："用科技感风格生成图片"
AI：已选中【科技感】风格，请描述一下你想生成的内容？
用户："一张科技公司官网banner"
AI：正在提炼提示词...
    原始描述：一张科技公司官网banner
    提炼后：科技感官网Banner，深蓝渐变背景，流动光效，现代简约风格
AI：融合风格生成中...
```

### 纯文字生成（不用风格）
```
用户："生成一张赛博朋克风格的城市夜景"
```
AI 直接调用 `generate`。

---

### 第1步：提取风格（可命名）

用户发来图片时，自动提取风格并保存。支持**给风格起名字**，方便后续引用。

```
python3 scripts/doubao_image_ops.py analyze <图片路径或URL> --name=风格名称
```

**示例：**
```
# 不命名 → 系统自动分配编号 #003
analyze /path/to/image.jpg

# 起名字 → 保存为"科技感"
analyze /path/to/image.jpg --name=科技感
```

**返回示例：**
```
✅ 风格分析完成
   风格编号：#003（名称：科技感） → 已保存
```

---

### 第2步：指定风格 + 描述生成图片

用 `use` 命令加载已有风格，与文字描述融合后生成图片。

```
python3 scripts/doubao_image_ops.py use <风格编号或名称> "<生图描述>" [--size 2K] [--no-watermark]
```

**注意**：用户说"用xxx风格生成图片"时，**不要直接拼凑生成**，而是先确认风格并询问主题描述。

**支持两种引用方式：**
- **按编号**：`use 3 "<描述>"`（引用 #003）
- **按名称**：`use 科技感 "<描述>"`（模糊匹配名称）

**示例：**
```
# 用户："用科技感风格生成图片"
# AI 确认风格后询问：
#   已选中【科技感】风格，请描述你想生成的内容？
# 用户："一张科技公司官网banner"
# AI → use 科技感 "一张科技公司官网banner" → 生成

# 直接指定（底层命令）：
use 3 "一张赛博朋克风格的城市夜景海报"
use 科技感 "未来感十足的程序员头像，蓝色调"
```

---

### 其他命令

以下为底层脚本命令（AI 自动调用，用户无需感知）：

**纯文字生图（不用风格）：**
```
python3 scripts/doubao_image_ops.py generate "<生图描述>" [--size 2K] [--no-watermark]
```

**查看已保存的风格列表：**
```
python3 scripts/doubao_image_ops.py list
```

**返回示例：**
```
📋 已保存的风格：
  #001【SKILL写作指南】  这张图的核心信息可以整理如下...
        保存于 2026-03-23 22:16:30
  #003【科技感】  画面以冷色调为主，蓝色发光元素...
        保存于 2026-03-24 18:00:00
```

---

## 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--name=名称` | 提取风格时给风格起名 | （空） |
| `--size=尺寸` | 生图尺寸，支持 1K/2K/4K、16:9/4:3/1:1、1920x1080 | 2K |
| `--no-watermark` | 关闭水印 | 水印开启 |
| `--format` / `-f` | 格式化输出（人类可读） | JSON输出 |

---

## ⚠️ 重要：风格文件只读

**风格文件（style_XXX.json）是只读的，不要修改。**

生成图片时：
- **输入**：用户提供的描述 + 已保存的风格描述
- **行为**：AI 把两者融合成生图 prompt，调用生图模型
- **禁止**：不要把用户描述写入风格文件，不要修改 style_description

### AI 提炼提示词

在融合之前，AI 先分析用户描述：
- 提取核心内容和构图意向
- 保留关键视觉元素
- 去除模糊、冗余的表达
- 输出一个清晰、精准的生图提示词

### 融合公式
```
最终 prompt = 提炼后的描述 + "，风格要求：" + style_description
```

风格文件一旦保存，内容固定不变。只有 `analyze` 命令会写入新风格。

---

## 返回格式

### 风格分析结果
```json
{
  "success": true,
  "style_description": "详细风格描述...",
  "style_id": 3,
  "style_path": "scripts/styles/style_003.json"
}
```

### 图像生成结果
```json
{
  "success": true,
  "image_url": "https://...",
  "task_id": "...",
  "style_id": 3,
  "fused_prompt": "用户描述，风格要求：..."
}
```

---

## 注意事项

- 生图模型为异步API，脚本会自动轮询直到完成（约30-90秒）
- 图片默认带水印，可通过 `--no-watermark` 关闭
- 支持的生图尺寸：1K, 2K, 4K（默认2K）
- API Base URL：https://ark.cn-beijing.volces.com/api/v3

---

*doubao-image-ops v2.2 - 2026-03-24*
